[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

# Daily recap email — runs every morning at 9am ET
[[crons]]
  schedule = "0 14 * * *"
  path = "/api/cron/recap?secret=${CRON_SECRET}"
